# Local Migration Fixture

Small v7.1 fixture for exercising the local init/status/run/resume flow without external dependencies or API calls.

The task asks Forgis to migrate a single greeting label from the toy SwiftUI-style source file into the toy target file under `target-output/`.
