# Platform Prompt: Android

The target platform is Android.

Prefer Android-native user experience and idiomatic Android project structure.

If the configured target stack is `kotlin-compose`, prefer Kotlin and Jetpack Compose.

Do not invent signing configs, private keystores, Play Store metadata, package names, or credentials.
