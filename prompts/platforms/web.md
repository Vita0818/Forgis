# Platform Prompt: Web

The target platform is Web.

If the configured target stack is `web-react`, prefer TypeScript, React, and Vite.

Do not invent backend services, API keys, or deployment credentials.
