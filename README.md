# Forgis

Forgis is a cloud-based migration forge for transforming Apple-platform projects into Android, Windows, and other platform repositories.

It uses:

- GitHub Actions as the cloud automation runner
- Migration rules as the toolset
- DeepSeek API as the model power source
- GitHub repositories as source and output targets

Forgis does not run on the user's Mac and does not access local files.
